'use client';
import React from 'react'

const Loader = () => {
  return (
    <div className='bg-red-500 h-screen w-screen z-50 flex items-center justify-center'>Wait for it...</div>
  )
}

export default Loader
